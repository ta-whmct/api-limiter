[project]
name = "api-limiter"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ruff>=0.16.2",
]

[tool.ruff]
fix = true
unsafe-fixes = true
src = ["app"]
exclude = [".venv"]

line-length = 100
indent-width = 4
include = ["ALL"]

